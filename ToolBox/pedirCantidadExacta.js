// PEDIR X CANTIDAD AL USUARIO
const MAX_PEDIDOS = 5;
let precio;
let  contadorFactura= 0;
const  MENSAJE_PEDIR = "MENSAJE DE PEDIR AL USUARIO";

while(contadorFactura < MAX_PEDIDOS){

    contadorFactura = contadorFactura + 1;    
}